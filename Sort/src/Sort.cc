/**@file Sort.cc
 * @author Caleb Reister <calebreister@gmail.com>
 */

#include <array>
#include <fstream>
#include <iostream>
#include <cstdlib>
#include <chrono>

namespace sort {
    template<std::size_t SIZE>
    void radix(std::array<int, SIZE>& a);

    template<std::size_t SIZE>
    void merge(std::array<int, SIZE>& a);

    template<std::size_t SIZE>
    void quick3(std::array<int, SIZE>& a);
}

int main() {

}

template<std::size_t SIZE>
void sort::radix(std::array<int, SIZE>& a) {

}

template<std::size_t SIZE>
void sort::merge(std::array<int, SIZE>& a) {

}

template<std::size_t SIZE>
void sort::quick3(std::array<int, SIZE>& a) {

}
